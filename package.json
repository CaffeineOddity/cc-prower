{
  "name": "cc-power-monorepo",
  "version": "1.0.0",
  "description": "Lightweight bridge between Claude Code and chat platforms",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test"
  },
  "keywords": [
    "claude",
    "feishu",
    "telegram",
    "whatsapp",
    "mcp"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}